import React from 'react';import { Trash2 } from 'lucide-react';export default function AdminInvitationsTable({ invitations, handleDelete }) {    return (        <div className="card" style={{ padding: 0, overflow: 'hidden' }}>            <table style={{ width: '100%', borderCollapse: 'collapse' }}>                <thead>                    <tr style={{ background: '#f8f8f8', borderBottom: '1px solid #eee' }}>                        <th style={{ textAlign: 'left', padding: '15px' }} className="label">Member Detail</th>                        <th style={{ textAlign: 'left', padding: '15px' }} className="label">Role / job</th>                        <th style={{ textAlign: 'left', padding: '15px' }} className="label">Status / Expires</th>                        <th style={{ textAlign: 'right', padding: '15px' }} className="label">Actions</th>                    </tr>                </thead>                <tbody>                    {invitations.map(invite => (                        <tr key={invite.id} style={{ borderBottom: '1px solid #f0f0f0' }}>                            <td style={{ padding: '15px' }}>                                <div style={{ fontWeight: '700' }}>{invite.email}</div>                                <div style={{ fontSize: '12px', color: '#999' }}>Token: {invite.token?.substring(0, 8)}...</div>                            </td>                            <td style={{ padding: '15px' }}>                                <div style={{ textTransform: 'capitalize', fontWeight: 'bold' }}>{invite.role}</div>                                <div style={{ fontSize: '13px', color: '#666' }}>{invite.job_title || 'No title set'}</div>                            </td>                            <td style={{ padding: '15px' }}>                                <span className={`role-badge`} style={{ background: '#eee', color: '#666' }}>                                    Pending                                </span>                                <div style={{ fontSize: '11px', color: '#999', marginTop: '5px' }}>                                    Exp: {new Date(invite.expires_at).toLocaleDateString('fr-FR')}                                </div>                            </td>                            <td style={{ padding: '15px', textAlign: 'right' }}>                                <button                                    onClick={() => handleDelete(invite.id)}                                    style={{ background: 'none', border: 'none', color: '#38a169', cursor: 'pointer', fontWeight: 'bold', display: 'flex', alignItems: 'center', gap: '5px', marginLeft: 'auto', fontSize: '11px' }}                                >                                    <Trash2 size={14} /> REVOKE                                </button>                            </td>                        </tr>                    ))}                    {invitations.length === 0 && (                        <tr>                            <td colSpan="4" style={{ padding: '60px', textAlign: 'center', color: '#999' }}>No pending member invitations found.</td>                        </tr>                    )}                </tbody>            </table>        </div>    );}
